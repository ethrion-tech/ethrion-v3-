(function(){const rc=document.getElementById('radar');if(!rc)return;const ctx=rc.getContext('2d');const inputs=[...document.querySelectorAll('input[name^="c"]')];const TWO=Math.PI*2;function vals(){return inputs.map(i=>Math.max(0,Math.min(1,(+i.value||0)/100)));}function draw(v){const W=rc.width=rc.clientWidth*2,H=rc.height=rc.clientHeight*2;ctx.setTransform(2,0,0,2,0,0);const cx=W/4,cy=H/4,R=Math.min(W/2,H/2)*.38,N=8;ctx.clearRect(0,0,W/2,H/2);for(let r=1;r<=5;r++){const rr=R*r/5;ctx.beginPath();for(let i=0;i<N;i++){const a=-Math.PI/2+i*(TWO/N);const x=cx+Math.cos(a)*rr;const y=cy+Math.sin(a)*rr;i?ctx.lineTo(x,y):ctx.moveTo(x,y);}ctx.closePath();ctx.strokeStyle='rgba(255,255,255,.12)';ctx.stroke();}for(let i=0;i<N;i++){const a=-Math.PI/2+i*(TWO/N);const x=cx+Math.cos(a)*R;const y=cy+Math.sin(a)*R;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(x,y);ctx.strokeStyle='rgba(255,255,255,.1)';ctx.stroke();}ctx.beginPath();for(let i=0;i<N;i++){const a=-Math.PI/2+i*(TWO/N);const r=R*v[i];const x=cx+Math.cos(a)*r;const y=cy+Math.sin(a)*r;i?ctx.lineTo(x,y):ctx.moveTo(x,y);}ctx.closePath();const g=ctx.createLinearGradient(0,0,W/2,H/2);g.addColorStop(0,'rgba(125,211,252,.35)');g.addColorStop(1,'rgba(167,139,250,.35)');ctx.fillStyle=g;ctx.fill();ctx.lineWidth=2;ctx.strokeStyle='rgba(167,139,250,.85)';ctx.stroke();}inputs.forEach(i=>i.addEventListener('input',()=>draw(vals())));draw(vals());})();